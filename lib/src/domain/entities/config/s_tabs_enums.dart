enum STabAnimationType {
  fade,
  slide,
  scale,
}

enum STabType {
  line,
  card,
  editableCard,
}

enum STabPosition {
  top,
  right,
  bottom,
  left,
}

enum STabSize {
  small,
  medium,
  large,
}
