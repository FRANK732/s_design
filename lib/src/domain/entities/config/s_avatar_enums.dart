/// Defines the predefined size variants for [SAvatar].
///
/// If a numeric size is needed instead, you can pass a custom `double` to the component.
enum SAvatarSize {
  large,
  middle,
  small,
  custom,
}

/// Defines the shape of the [SAvatar].
enum SAvatarShape {
  circle,
  square,
}
